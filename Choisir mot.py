import random
def random_word():
        possible_words = ['becode', 'learning', 'mathematics', 'sessions']
        for word in range(3):
            return print(random.choice(possible_words)) 
random_word()

if letter in self.possible_word:
                for i in range(len(self.possible_word)):
                    if self.possible_word[i] == letter:
                        self.correctly_guessed_letters[i] = letter
                        self.word_to_find[i] = letter